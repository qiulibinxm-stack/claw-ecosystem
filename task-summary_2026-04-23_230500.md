# 五行看板知识库功能开发计划

## Objective
为五行星舰指挥台添加完整的知识库展示和交互功能，分Phase A（纯前端）和Phase B（后端API）两阶段实现。

## Key Reasoning

### Phase A — 纯前端快速上线
**目标**：静态数据嵌入式实现，立即可用

**已完成**：
- ✅ 左侧栏「知识库」状态卡片（总条数/分类/最近添加）
- ✅ 中心区域「知识库分类面板」（8分类卡片网格）
- ✅ KNOWLEDGE_CATEGORIES基础数据

**待完成**：
1. **KNOWLEDGE_DATA完整嵌入**（41条知识）
   - ai/ (12条)
   - tech/ (11条)
   - workflow/ (7条)
   - ideas/ (4条)
   - lessons/ (2条)
   - yijing/ (1条)
   - evolution/ (1条)
   - agency/ (3条)

2. **分类详情弹窗**
   ```html
   <div class="modal-overlay" id="kbCategoryModal">
     <!-- 展示该分类所有知识条目 -->
   </div>
   ```

3. **知识详情面板**
   ```html
   <div class="modal-overlay" id="kbDetailModal">
     <!-- 标题 + 日期 + 标签 + 摘要 + 文件路径 -->
     <!-- Phase B: 完整内容 -->
   </div>
   ```

4. **搜索功能**
   ```js
   function searchKnowledge(query) {
     // 遍历KNOWLEDGE_DATA全文检索
     // 匹配title/summary/tags
   }
   ```

### Phase B — 后端API升级
**目标**：实时读取、完整功能

**API设计**：
```
GET /api/kb/list           → 列出所有知识（分类+文件名）
GET /api/kb/read?path=... → 读取知识完整内容（返回markdown）
GET /api/kb/search?q=...   → 搜索知识（返回匹配列表）
POST /api/kb/add           → 新增知识（写入文件）
```

**Flask后端框架**：
```python
from flask import Flask, jsonify, request
import os, glob

app = Flask(__name__)
KB_ROOT = 'C:/Users/Administrator/.qclaw/workspace-agent-cf443017/knowledge'

@app.route('/api/kb/list')
def kb_list():
    # 扫描knowledge目录，返回结构化数据
    pass

@app.route('/api/kb/read')
def kb_read():
    # 读取指定路径的md文件内容
    pass

@app.route('/api/kb/search')
def kb_search():
    # 全文搜索（支持标题/摘要/标签）
    pass
```

## Conclusions

### 今日成果（2026-04-23）
- ✅ 五行星舰指挥台v1.0完成（深海星舰美学）
- ✅ 三栏布局（左侧栏+中栏+右栏）
- ✅ 左侧栏：投喂入口+备份状态+知识库状态+八字系统
- ✅ 中心栏：五行Agent卡+Kanban看板+知识库分类面板
- ✅ 右栏：实时对话面板
- ✅ Kanban三栏：运行看板+消耗看板+备份看板

### 明日工作（2026-04-24）
1. KNOWLEDGE_DATA数据嵌入（41条完整信息）
2. 分类详情弹窗实现
3. 知识详情面板实现
4. 搜索功能实现
5. （时间允许）Flask后端API框架

### 文件路径
- 看板文件：`C:\Users\Administrator\.qclaw\workspace-agent-cf443017\projects\wuxing-dashboard\index.html`（70KB）
- 知识库目录：`C:\Users\Administrator\.qclaw\workspace-agent-cf443017\knowledge\`（41个.md文件）

---

**时间**：2026-04-23 23:05
**状态**：Phase A数据准备中，明日继续
